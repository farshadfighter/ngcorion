# NOTE: not currently read by any code — src/config/api.js uses a relative
# baseURL ('') so requests go to the same origin (/api/..., /auth/...) that
# served the app. Kept here for documentation / future use only.
VITE_API_URL=https://ngcorion.com
