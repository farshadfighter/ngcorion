# Netease Project Dependencies
# Install with: pip install -r requirements.txt

# Web Framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.12.0

# Authentication & Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Pin bcrypt < 4.1: passlib 1.7.4 reads bcrypt.__about__.__version__ (removed in
# 4.1) and bcrypt >= 4.1 raises instead of truncating at 72 bytes, which breaks
# all password hashing/verification. Keep this until passlib is upgraded.
bcrypt>=4.0.1,<4.1.0

# Configuration
pydantic-settings>=2.0.0
pydantic[email]
python-dotenv>=1.0.0

# SSH & Network Device Management
netmiko>=4.2.0
paramiko>=3.3.0

# CIS MongoDB benchmark module (app/cis/services/mongodb.py)
pymongo>=4.0.0
PyYAML>=6.0

# Data Export (Excel)
openpyxl>=3.1.0

# SQL Server connectivity
pymssql>=2.2.0

# Windows Server WinRM connectivity
pywinrm>=0.4.3

# License system
cryptography>=43.0.0
requests>=2.31.0
